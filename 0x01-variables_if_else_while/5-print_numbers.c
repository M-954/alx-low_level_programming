#include <stdio.h>
/**
 * main - prints all base 10 numbers
 *
 * Returns: 0 Always (success)
 */
int main(void)
{
	int g = 0;

	while (g < 10)
	{
		printf("%d", g);
		g++;
	}
	printf("\n");
	return (0);
}	
