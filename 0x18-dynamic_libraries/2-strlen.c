#include "main.h"
#include <string.h>
/**
 * _strlen - outputs length of a string
 * @s: string character
 *
 * Return: length of string
 */
int _strlen(char *s)
{
	int len = strlen(s);

	return (len);
}
